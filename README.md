# multi-area-ospf-acl-security-policies
Extending a 3-router multi-area OSPF topology with extended ACLs to enforce per-department access policies (Sales, Guest, IT) to a shared server — verified with OSPF adjacency checks, interface-applied ACLs, and per-service connectivity tests.
